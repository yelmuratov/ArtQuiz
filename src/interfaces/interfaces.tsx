export interface Idata {
  author: string
  name: string
  year: string
  imageNum: string
}

export interface Icategory {
    categoryName: string,
    ArtistQuizDone: boolean,
    PicturesQuizDone:boolean,
    artistScore: number,
    picturesScore: number,
}